{% extends 'base.html.twig' %}

{% block title %}EasyAd - Ajout discipline{% endblock %}

{% block body %}
<div class="row justify-content-sm-center my-5 ">
    <div class="border border-primary p-2 rounded bg-light p-5">
        {{ form_start(form) }}
        <div class="form-group">
            <h>{{ form_label(form.name,'Nom Discipline',{'label_attr':{'class':'text-dark'}}) }}</h>
            {{ form_widget(form.name,{'attr':{'class':'form-control','placeholder':'Entrer le nom de la discipline'}}) }}
        </div>
        <div class="row justify-content-sm-center">
            {{ form_row(form.submit,{'attr':{'class':'btn btn-primary mx-auto'}}) }}
        </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}